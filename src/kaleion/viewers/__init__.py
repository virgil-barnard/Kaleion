"""Optional viewers of captured results; importing the core needs no viewer extras.

Install with ``python3 -m pip install -e '.[notebooks]'``. Import the Plotly
and video adapters explicitly from their submodules.
"""
